module.exports = {
  /**
   * Create a text file and prompt the user to download it.
   * @param  {string} textContent - Text content of the file.
   * @param  {string} mimeType - Mime type of the download.
   */
  createTextFileForDownload: (textContent, mimeType) => {
    //TODO
  }
}
